"""Smoke-модуль для проверки инфры devboard (MCP, subagent, канбан)."""


def greet(name: str) -> str:
    """Вернуть приветствие на русском.

    Поведение на пустой строке: возвращает "Привет, !" — явно задокументировано.
    """
    return f"Привет, {name}!"
